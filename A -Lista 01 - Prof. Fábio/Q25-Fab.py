# Entrada 

m = int (input ('Quantos metros(m): '))

# Processamento

km = m // 1000
metros_sobrando = m % 1000

# Saída 

print (f'{m} metros(m) equivalem a {km} quilometros(km) e {metros_sobrando} metros(m). ')